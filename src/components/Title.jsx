function Title({ title }) {
  return (
    <div className="title">
      <h2 className="fw-semibold text-center text-2xl">{title}</h2>
      <div className="underline"></div>
    </div>
  );
}
export default Title;